server:
  port: 9003 #9004

spring:
  application:
    name: nacos-payment-provider  #集群
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'