###consul服务端口号
server:
  port: 80

spring:
  application:
    name: A-10-cloud-consumerCS-order-80 #必须字母开头
  ####consul注册中心地址
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        #hostname: 127.0.0.1
        service-name: ${spring.application.name}



